// 재화를 글자 안에 그림으로 넣는 태그.
//
// 포인트는 한때 최고 해금 등급 슬라임 그림으로 표시했다. 필드의 슬라임과 같은 그림이라
// 재화인지 슬라임인지 헷갈렸고, 등급이 오를 때마다 그림이 바뀌어 같은 재화로 보이지
// 않았다. 전용 아이콘 하나로 고정한다.
//
// 그림은 TMP 기본 스프라이트 에셋(SlimeNormal20)에 들어 있다. 이름을 바꾸면 여기와
// 스프라이트 아틀라스를 함께 바꾼다.
//
// TMP는 스프라이트를 글자 높이에 맞춰 늘린다. 슬라임 그림은 위아래 여백이 있어
// 그대로 맞았지만 코인은 캔버스를 꽉 채워서 같은 태그로는 숫자보다 크게 나온다.
// 이미지 해상도를 줄여도 높이에 맞춰 다시 늘어나므로 여기서 줄이고, 작아진 만큼
// 기준선 쪽으로 내려앉는 것을 들어 올린다. 씬의 보상 버튼 글자도 같은 표기를 쓴다.
public static class CurrencyIcon
{
    public const string Point =
        "<size=60%><voffset=0.12em><sprite name=\"PointIcon\"></voffset></size><space=0.15em>";
}
